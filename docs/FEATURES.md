# Feature status

Statuses describe the code in this repository, not a launch or security certification. The canonical production tracker is `docs/MYJOURNEY_PRODUCTION_MASTER_PLAN.md`; the older roadmap is retained as historical evidence only.

| Area | Status | Engineering truth |
| --- | --- | --- |
| Public home/navigation/footer | Implemented | React shell, CMS-backed contexts, navigation, and public landing sections exist. |
| Articles | Implemented | Public/CMS flows, bounded server pagination/filter/search/sort, categories, tags, comments, API-authoritative bodies/metrics, Free/Premium serialization, SEO/social metadata, and explicit unavailable/retry states exist. Article list/detail routes force the Article discriminator so Story records cannot expose unsupported Article-only Like/Bookmark/Save controls. Phase 5 Step 1 established the canonical 5-category taxonomy (Life, Reflections, Experiences, Lessons, Travel), safe legacy Article soft-archival (Migration 013), zero-prose tombstone responses with noindex metadata, Experiences editorial provenance, Travel verification schemas, and Phase 5 seeder foundation. Phase 5 Step 2A adds the first canonical batch of 10 articles (5 Life, 5 Reflections; 2 Pillars at 9,000+ words, 8 Longforms at 6,000+ words; 66,257 total words across 1,180 structured blocks), authored under the canonical byline "MyJourney Editorial", fully verified via editorial audit tests, MongoDB seeding, and multi-viewport browser QA. |
| Stories | Implemented | Structured sections, validation, reading time, dedicated routes, 30 stable presets over six shared engines, CMS/public preview reuse, structured quote/media metadata, SEO, and legacy compatibility exist. Structural preset verification is documented; browser visual QA is tracked separately. |
| Stories | Implemented | Canonical launch catalog (8 original stories, 35,718 words, 121 sections) with zero text duplication across three production batches, structured section validation (including dialogue and callouts), reading time calculated at 200 wpm from normalized readable text, server-authoritative Free/Premium body protection, sequential next/prev story navigation, reflection questions, dynamic category discovery, accessible reading progress tracking (`role="progressbar"`), 30 stable presets over six shared engines, CMS/public preview reuse, structured quote/media metadata, and SEO metadata. Structural preset verification and editorial audits pass; browser visual QA is tracked separately. |
| Stories | Implemented | Phase 4 human Life Stories pivot: Character-driven literary fiction catalog (Stories 1–5 launched in Batch 1: 26,343 words across 61 structured sections), zero text duplication, structured section validation, reading time calculated at ~200 wpm, server-authoritative Free/Premium body protection (4 Free / 1 Premium in Batch 1), sequential navigation, timeless messaging (removed false daily publishing claims), removal of instructional reflection questions, dynamic category discovery, accessible progress tracking (`useStoryReadingProgress`), ring-fenced Story Continue Reading via `StoryProgressService` (never affecting Article stats/streaks), non-public Character Bibles in `server/data/storyBibles/`, and archived legacy stories safe tombstone behavior. |
| Stories | Implemented | Canonical 20-story character-driven literary catalog (Phase 4 complete: Batches 1–4, 175,505 words, 360 structured sections, ~44 min average read time, 16 Free / 4 Premium, exactly 2 stories per locked layout preset), zero text duplication across 5,037 paragraphs, structured section validation (including chapters, quotes, dialogue, callouts, and reflections), reading time calculated at 200 wpm from normalized readable text, server-authoritative Free/Premium body protection (unauthenticated users receive excerpt-only preview with zero prose leaked; entitled users receive full content), sequential next/prev story navigation, timeless messaging (removed fake daily publishing claims), dynamic category discovery, accessible reading progress tracking (`role="progressbar"`), ring-fenced Story Continue Reading via `StoryProgressService` (never affecting Article stats/streaks), distinct visual identities with multi-tier category image fallbacks, 22 legacy/demo stories safely archived with 200 OK tombstone responses, non-public Character Bibles in `server/data/storyBibles/`, and CMS seed decoupling. Structural preset verification and editorial audits pass (100% pass, 0 errors, 0 duplication); visual browser QA verified. |
| Reader profile and Article progress | Implemented / migration required | Authenticated profile DTO, Reader-owned Like/Bookmark/Save interactions with response-driven Profile synchronization, atomic per-user/per-Article progress, real Continue/Completed/time/achievement states, public native/copy-link sharing, and Story exclusion exist. Migration 011 must be reviewed/applied per environment before rollout. |
| Core browser reliability smoke | Implemented foundation | Isolated Playwright/Chromium fixtures exercise login, Header, Article Like/Unlike/Bookmark/Save/Share/progress, Profile synchronization, Light/Dark state, account isolation, logout, and mobile drawer. The wider browser inventory remains open; Story Save is known non-persistent. |
| Authentication | Implemented | Registration, normalized email/full-E.164 mobile identity, hashed single-use OTP, throttling/lockouts, enumeration-resistant recovery, cookie sessions, atomic refresh rotation/replay rejection, password reset/change, and a reauthenticated OTP-verified email/mobile change flow that revokes prior sessions exist. SMTP/SMS delivery depends on configuration. |
| MyJourney Premium entitlement | Implemented | One account-level Premium plan grants the entitlement catalog for 1/3/6/12-month billing durations. |
| Premium checkout/billing sync | Test-mode lifecycle implemented / provider validation required | Fixed INR/USD server prices, Money, auditable billing records, Razorpay test checkout, signed webhooks, transactional prepaid activation/renewal, replay/concurrency protection, automatic date-based expiration, cancellation preserving paid access, payment-issue status, and purchase-scoped full-refund revocation exist. Existing subscription dashboard displays authoritative dates and prepaid status. Live mode and recurring provider management remain unavailable; real provider/browser QA remains required. |
| MyJourney Life | Implemented / privacy browser QA required | Private Premium APIs/UI cover Today, habits, routines, tasks, goals, health, money, journal, insights, search, reports, notifications, export, and deletion. Offline schema v2 queues only minimal task creation and non-sensitive habit/task/goal-action events for the current owner for at most 24 hours; sensitive Life changes require a connection. |
| Life web push | Implemented / requires external configuration | Requires valid VAPID credentials. |
| Life AI review | Implemented / requires external configuration | Requires `LIFE_AI_ENABLED=true` and an enabled AI provider; deterministic reports remain available. |
| Calendar/health-device integrations | Foundation only | Capability registries exist; no real adapter is connected by default. |
| Creator applications/review | Implemented | Applicant workflow and Admin review/history are server-authorized. |
| Creator directory/profiles/follows | Implemented | Public active profiles, shelves, owner state, follow/unfollow, dedupe, and self-follow prevention exist. |
| Creator Studio | Implemented | Active-Creator profile, content, course/curriculum, analytics, and media-metadata workflows exist. |
| Creator economy | Foundation only | Analytics/earnings/ledger models and inactive UI state exist; no real earnings or payouts are produced. |
| Topics | Implemented | Public Topics and Admin management exist independently of Creators/content types. |
| Learn home/catalog/search | Implemented | Topics and Free/Premium catalogs are exposed through `/api/learn`. |
| Courses/lessons | Implemented | Curriculum, public previews, Premium lessons, enrollment, progress, and Continue Learning exist. |
| Video metadata/catalog | Implemented | Metadata, ownership, access serialization, and catalog/detail UI exist. |
| Video streaming/upload | Deferred | No configured direct upload, adaptive streaming, signed delivery, or scanning provider. |
| Podcasts | Implemented / provider-dependent delivery | Series/episode metadata, catalogs, access checks, and transcript/show-note serialization exist; protected playback delivery is unavailable without a media provider. |
| Learning resources | Implemented / provider-dependent delivery | Metadata/catalog/access checks exist; Premium resources require protected assets and real delivery is unavailable. |
| Exams | Foundation only | Exam metadata/catalog exists; `assessmentEngineAvailable` is intentionally false. |
| Notifications | Implemented / requires provider configuration | In-app and scheduled flows exist; email, SMS, and web push depend on configured providers. |
| CMS/Admin | Implemented | `/cms/*` and management APIs require server-side Admin authorization; public runtime theme/feature/page/navigation/form/SEO contracts are separated from drafts and management metadata. Theme writes use validated design tokens, critical page/card/panel text contrast checks, stale-safe Light/Dark preview, and no raw CSS/JavaScript injection. Some enterprise modules remain capability-gated. |
| Launch readiness console | Implemented / environment blocked until configured | Admin-only live audits are read-only and fail closed on missing critical evidence. Release, deployment, and test history is never populated with sample records. |
| SEO dashboard and public metadata | Implemented | Scores/counts are derived from published records; absent evidence is reported as not calculated. Sitemap and JSON-LD exclude draft, private, deleted, and missing content. |
| AI platform | Implemented / requires external configuration | Provider registry/adapters exist; no provider means explicit 503 rather than fabricated output. Legacy CMS completions are Admin-only; reader assistant traffic uses the Agent domain. |
| News integrations | Implemented / requires external configuration | External news providers require their API keys. |
| Play Life | Implemented | Client engine, content, persistence, and tests exist. |
| Play With Friends | Implemented | Room APIs, guest identity, Socket.IO, games, metrics, QR invites, and integration tests exist. |
| Multiplayer persistent authority | Implemented | Default runtime rooms are Mongo-only and return 503 when storage disconnects; in-memory repositories are test/load harnesses only. |
| Redis multiplayer scaling | Implemented / requires external configuration | Local mode can run in process; horizontally scaled production needs Redis. |
| Life Auction | Implemented | Timed auction engine, modes, content packs, realtime, and load/integration tests exist. |
| Secret Vault | Implemented / requires external configuration | AES-256-GCM storage exists and is disabled without an explicit 32-byte key. |
| Production observability | Implemented foundation | Request IDs, structured route-template latency/error logs, hashed user identifiers, and audit-diff redaction exist. Central collection/alerting and distributed metrics remain deployment work. |
| Social login | Deferred | No authentication provider route is configured; the client reports unavailable. |
| MyJourney Agent | Implemented | Server-authoritative unified Agent (`/api/agent/v1`), persistent conversation state, Zod-validated tool registry, permission & entitlement enforcement, cryptographic single-use confirmation tokens, idempotent message delivery, telemetry & audit redaction, and `MockAgentProvider` / `LocalAgentProvider`. |
| Agent Voice (Talk to MyJourney) | Implemented / requires browser-device QA | Browser speech-to-text / text-to-speech integration into the same authenticated Agent conversation pipeline with press-to-talk state machine, stop actions, and zero background audio storage. Source contracts do not replace microphone, speech-engine, browser, device, or accessibility testing. |

When capability status changes, update this inventory in the same change as code and tests.
